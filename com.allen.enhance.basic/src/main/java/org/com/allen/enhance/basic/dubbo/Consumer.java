package org.com.allen.enhance.basic.dubbo;import com.alibaba.dubbo.config.ApplicationConfig;import com.alibaba.dubbo.config.ReferenceConfig;import com.alibaba.dubbo.config.RegistryConfig;import com.alibaba.dubbo.rpc.service.GenericService;public class Consumer {    public static void main(String[] args) {        // 当前应用配置        ApplicationConfig application = new ApplicationConfig();        application.setName("allen-demo");        // 连接注册中心配置        RegistryConfig registry = new RegistryConfig();        registry.setAddress("zookeeper://127.0.0.1:2181");        registry.setClient("curator");        // 引用远程服务        ReferenceConfig<UserService> reference = new ReferenceConfig<>(); // 此实例很重，封装了与注册中心的连接以及与提供者的连接，请自行缓存，否则可能造成内存和连接泄漏        reference.setApplication(application);        reference.setRegistry(registry); // 多个注册中心可以用setRegistries()        reference.setInterface(UserService.class);        reference.setVersion("0.0.1");        reference.setClient("failover"); // 设置容错        reference.setLoadbalance("");// 设置负载均衡        // 和本地bean一样使用xxxService        UserService userService = reference.get(); // 注意：此代理对象内部封装了所有通讯细节，对象较重，请缓       // GenericService genericService = (GenericService) reference.get();       // genericService.$invoke()        System.out.println("======================star=============================");        userService.sayHello("hello world");        System.out.println("=======================end============================");    }}